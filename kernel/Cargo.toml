[package]
name = "kernel"
version = "0.1.0"
edition = "2021"
publish = false
links = "entry"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
paste = "1.0"

[build_dependencies]
toml = "0.8"
